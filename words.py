"""
import allowedanswers as answers, allowedguesses as guesses
from get_wordlists import answers_count, guesses_count

a_list = []
for i in range(answers_count):
    a_list.append(answers[i])

print(a_list[0])
"""